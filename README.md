# 🛒 Shop VIP Bot

یک ربات فروشگاهی تمام اتوماتیک تلگرام نوشته شده با PHP، مناسب برای فروش محصولات دیجیتال با درگاه پرداخت. این ریپازیتوری دارای یک اسکریپت نصب خودکار (Auto-Installer) است که راه‌اندازی ربات را روی سرورهای لینوکسی (مخصوصاً Ubuntu 22.04) در کمتر از ۲ دقیقه انجام می‌دهد.

## ✨ امکانات ربات
* فروشگاه کاملاً خودکار با تحویل آنی محصول پس از پرداخت
* سیستم کیف پول و الماس (کوین)
* درگاه پرداخت تعبیه شده (Payping)
* پنل مدیریت حرفه‌ای داخل تلگرام (افزودن/حذف محصول، مدیریت موجودی کاربران و...)
* استفاده از فایل‌های متنی (txt) به عنوان دیتابیس (بدون نیاز به درگیری با MySQL)

## 🚀 پیش‌نیازهای نصب
قبل از اجرای اسکریپت، به موارد زیر نیاز دارید:
1. یک سرور مجازی با سیستم‌عامل **Ubuntu 22.04** (مثل سرورهای هتزنر).
2. دسترسی **Root** به سرور.
3. یک **دامین یا ساب‌دامین** (مثلاً `bot.yourdomain.com`) که از قبل به IP سرور شما متصل (Point) شده باشد.
4. **توکن ربات** از [@BotFather](https://t.me/BotFather).
5. **آیدی عددی تلگرام ادمین** (برای دسترسی به پنل مدیریت).

## 🛠 آموزش نصب سریع (Auto-Install)

برای نصب کامل ربات، پیش‌نیازها، تنظیمات وب‌سرور (Nginx)، دریافت SSL رایگان و اتصال Webhook، فقط کافیست دستور زیر را در ترمینال سرور خود کپی و اجرا کنید:

```bash
sudo bash <(curl -Ls https://raw.githubusercontent.com/Moriistar/shop-vip/main/install.sh)

